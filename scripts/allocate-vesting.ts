import { ethers } from "hardhat";

async function main() {
  // TODO: Deploy vesting escrows per allocation table and fund them
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
